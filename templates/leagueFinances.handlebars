<form action="/l/{{lid}}/league_finances" method="GET" class="form-inline pull-right">
  <select id="league-finances-select-season" name="season" class="season">
    {{#seasons}}
      <option value="{{season}}"{{#if selected}} selected="selected"{{/if}}>{{season}} season</option>
    {{/seasons}}
  </select>
</form>

<h1>League Finances {{new_window}}</h1>

<p>The current salary cap is <strong>${{round salaryCap 2}}M</strong>.</p>

<p>
  <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered table-condensed" id="league-finances">
  <thead>
    <tr><th>Team</th><th>Avg Attendance</th><th>Revenue (YTD)</th><th>Profit (YTD)</th><th>Cash</th><th>Payroll</th></tr>
  </thead>
  </table>
</p>